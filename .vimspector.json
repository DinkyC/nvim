{
  "configurations": {
    "Launch": {
      "adapter": "vscode-cpptools",
      "configuration": {
        "request": "launch",
        "MIMode": "gdb",
        "program": "${workspaceRoot}/main",
        "cwd" : "${workspaceRoot}",
        "externalConsole": true
      }
    }
  }
}
